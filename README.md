# Book API Using Flask

A simple RESTful API for managing a collection of books. The API allows users to create, read, update, and delete book entries with attributes such as title, price, and quantity. 
